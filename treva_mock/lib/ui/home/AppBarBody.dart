import 'package:flutter/material.dart';

class AppBarBody extends StatelessWidget{
  @override
  Widget build(BuildContext context){
    return new AppBar(
        title: new Text("Treva Home page"),
    );
  }
}
